<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>공지사항</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="notice-page">
    <div class="notice-container">
      <div class="notice-header">
        <h1 class="notice-title">공지사항</h1>
        <a href="/notice/new" class="notice-write-button">
          <i class="fas fa-pen"></i>
          글쓰기
        </a>
      </div>

      <ul class="notice-list">
        <% if (notices.length === 0) { %>
        <div class="notice-empty-message">
          <i class="fas fa-bell-slash"></i>
          등록된 공지사항이 없습니다.
        </div>
        <% } %> <% notices.forEach(notice => { %>
        <li class="notice-item">
          <a href="/notice/view/<%= notice.id %>"> <%= notice.title %> </a>
          <div class="notice-meta">
            <div class="notice-meta-item">
              <i class="fas fa-user"></i>
              <span><%= notice.author %></span>
            </div>
            <div class="notice-meta-item">
              <i class="fas fa-calendar"></i>
              <span
                ><%= new Date(notice.created_at).toLocaleDateString() %></span
              >
            </div>
            <div class="notice-meta-item">
              <i class="fas fa-eye"></i>
              <span><%= notice.views %></span>
            </div>
          </div>
        </li>
        <% }) %>
      </ul>

      <div class="notice-footer">
        <a href="/" class="notice-home-button">
          <i class="fas fa-home"></i>
          홈으로 돌아가기
        </a>
      </div>
    </div>
  </body>
</html>
