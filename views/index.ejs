<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>테무찬 - 새로운 쇼핑 경험</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <style>
      .featured-products {
        padding: 60px 5%;
        background: #f8f9fa;
      }

      .featured-title {
        text-align: center;
        margin-bottom: 40px;
      }

      .featured-title h2 {
        font-size: 2rem;
        color: #333;
        margin-bottom: 10px;
      }

      .featured-title p {
        color: #666;
        font-size: 1.1rem;
      }

      .featured-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 30px;
        max-width: 1200px;
        margin: 0 auto;
      }

      .featured-card {
        background: white;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
      }

      .featured-card:hover {
        transform: translateY(-5px);
      }

      .featured-image {
        height: 200px;
        overflow: hidden;
        position: relative;
      }

      .featured-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .featured-tag {
        position: absolute;
        top: 10px;
        left: 10px;
        background: #ff4757;
        color: white;
        padding: 5px 10px;
        border-radius: 15px;
        font-size: 0.8rem;
        font-weight: 600;
      }

      .featured-details {
        padding: 20px;
      }

      .featured-name {
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: 10px;
        color: #333;
      }

      .featured-price {
        color: #ff4757;
        font-weight: 700;
        font-size: 1.2rem;
      }

      .featured-price span {
        color: #999;
        text-decoration: line-through;
        font-size: 0.9rem;
        margin-right: 8px;
      }

      .featured-button {
        display: inline-block;
        margin-top: 15px;
        padding: 8px 20px;
        background: #ff4757;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        font-weight: 500;
        transition: background 0.3s ease;
      }

      .featured-button:hover {
        background: #ff6b81;
      }
    </style>
  </head>
  <body>
    <!-- 헤더 -->
    <header class="header">
      <a href="#" class="logo">
        <i class="fas fa-shopping-bag"></i>
        <div class="logo-text"><span>테무찬</span></div>
      </a>

      <div class="header-actions">
        <a href="/board" class="header-link">고객센터</a>
        <a href="/notice" class="header-link">공지사항</a>
      </div>
    </header>

    <!-- 메인 히어로 섹션 -->
    <section class="hero">
      <div class="hero-content">
        <h1>당신의 일상에 감각을 더하다</h1>
        <p>감각적인 아이템을 합리적인 가격에, 지금 테무찬에서 만나보세요.</p>
        <a href="/user/login" class="hero-button">로그인 하러가기</a>
      </div>
    </section>

    <!-- 추천 상품 섹션 -->
    <section class="featured-products">
      <div class="featured-title">
        <h2>오늘의 추천 상품</h2>
        <p>테무찬이 엄선한 특별한 상품들을 만나보세요</p>
      </div>

      <div class="featured-grid">
        <% if (featuredProducts && featuredProducts.length > 0) { %> <%
        featuredProducts.forEach(function(product) { %>
        <div class="featured-card">
          <div class="featured-image">
            <% if (product.image) { %>
            <img src="<%= product.image %>" alt="<%= product.name %>" />
            <% } else { %>
            <img src="/images/default-product.jpg" alt="기본 이미지" />
            <% } %>
            <div class="featured-tag">오늘의 추천</div>
          </div>
          <div class="featured-details">
            <h3 class="featured-name"><%= product.name %></h3>
            <div class="featured-price">
              <span><%= (product.price * 1.2).toLocaleString() %>원</span>
              <%= product.price.toLocaleString() %>원
            </div>
            <a href="/user/login" class="featured-button">자세히 보기</a>
          </div>
        </div>
        <% }); %> <% } else { %>
        <p style="text-align: center; grid-column: 1/-1; color: #666">
          현재 추천 상품이 없습니다. 곧 새로운 상품이 등록될 예정입니다.
        </p>
        <% } %>
      </div>
    </section>

    <!-- 푸터 -->
    <footer class="footer">
      <div class="footer-grid">
        <div class="footer-column">
          <h3>테무찬</h3>
          <p style="color: #bbb; margin-bottom: 20px; line-height: 1.7">
            테무찬은 더 나은 일상을 위한 쇼핑을 고민합니다. 믿을 수 있는 품질과
            합리적인 선택을 당신께 전합니다.
          </p>
        </div>

        <div class="footer-column">
          <h3>연락처</h3>
          <div class="footer-contact">
            <i class="fas fa-map-marker-alt"></i>
            <span>부산대학교 밀양캠퍼스 비마관</span>
          </div>
          <div class="footer-contact">
            <i class="fas fa-phone"></i>
            <span>010-7723-4412</span>
          </div>
          <div class="footer-contact">
            <i class="fas fa-envelope"></i>
            <span>mark7723@pusan.ac.kr</span>
          </div>
          <div class="footer-contact">
            <i class="fas fa-clock"></i>
            <span>평일 09:00 ~ 18:00 (주말, 공휴일 휴무)</span>
          </div>
        </div>
      </div>

      <div class="footer-bottom">
        <p>© 2025 테무찬. All Rights Reserved.</p>
      </div>
    </footer>
  </body>
</html>
