<!DOCTYPE html>
<html>
  <head>
    <title>로그인 실패</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        background-color: #f5f5f5;
      }
      .error-container {
        text-align: center;
        padding: 2rem;
        background-color: white;
        border-radius: 12px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        max-width: 400px;
        width: 90%;
      }
      .error-icon {
        font-size: 48px;
        margin-bottom: 1rem;
      }
      .error-message {
        color: #dc3545;
        margin-bottom: 1.5rem;
        font-size: 1.2rem;
        line-height: 1.5;
      }
      .button-container {
        display: flex;
        gap: 1rem;
        justify-content: center;
      }
      .back-button {
        display: inline-block;
        padding: 0.8rem 1.5rem;
        background-color: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 6px;
        transition: all 0.2s;
        font-weight: 500;
      }
      .back-button:hover {
        background-color: #0056b3;
        transform: translateY(-1px);
      }
      .home-button {
        display: inline-block;
        padding: 0.8rem 1.5rem;
        background-color: #6c757d;
        color: white;
        text-decoration: none;
        border-radius: 6px;
        transition: all 0.2s;
        font-weight: 500;
      }
      .home-button:hover {
        background-color: #5a6268;
        transform: translateY(-1px);
      }
      .error-type-username .error-icon::before {
        content: "👤";
      }
      .error-type-password .error-icon::before {
        content: "🔒";
      }
      .error-type-system .error-icon::before {
        content: "⚠️";
      }
    </style>
  </head>
  <body>
    <div class="error-container error-type-<%= errorType %>">
      <div class="error-icon"></div>
      <h2 class="error-message"><%= message %></h2>
      <div class="button-container">
        <a href="/user/login" class="back-button">다시 로그인하기</a>
        <a href="/" class="home-button">홈으로 가기</a>
      </div>
    </div>
  </body>
</html>
